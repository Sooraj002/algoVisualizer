#ifndef INTERPOLATION_SEARCH_H
#define INTERPOLATION_SEARCH_H

#include "visualizer.h"

// Performs interpolation search on a sorted array and visualizes the process
int interpolation_search(int* arr, int size, int target);

#endif // INTERPOLATION_SEARCH_H 